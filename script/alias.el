;; function cursor motion
(defalias 'eofu 'end-of-defun)
(defalias 'bofu 'begining-of-defun)
;; sexp cursor motion
(defalias 'kilexp 'kill-sexp)
(defalias 'markexp 'mark-sexp)
(defalias 'fexp 'forward-sexp)
(defalias 'bexp 'backward-sexp)

